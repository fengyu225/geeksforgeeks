#include "header.h"

void find_subarr(int arr[], int size, int sum){
    
}

int main(){
    int arr[] = {1, 4, 20, 3, 10, 5};
    find_subarr(arr,sizeof(arr)/sizeof(arr[0]), int sum);
    return 0;
}
